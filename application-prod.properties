# Production Profile for Quarkus Application

# Server Configuration
quarkus.http.port=${PORT:8080}
quarkus.http.host=0.0.0.0

# CORS Configuration
quarkus.http.cors=true
quarkus.http.cors.origins=${CORS_ORIGINS:*}
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.exposed-headers=*
quarkus.http.cors.access-control-max-age=24h
quarkus.http.cors.access-control-allow-credentials=true

# Database Configuration (MySQL)
quarkus.datasource.db-kind=mysql
quarkus.datasource.username=${DB_USERNAME}
quarkus.datasource.password=${DB_PASSWORD}
quarkus.datasource.jdbc.url=${DB_URL}
quarkus.datasource.jdbc.max-size=10
quarkus.datasource.jdbc.min-size=2
quarkus.datasource.jdbc.idle-removal-interval=5M
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.log.sql=false

# JWT Configuration
quarkus.smallrye-jwt.enabled=true
mp.jwt.verify.issuer=${JWT_ISSUER}

# JWT Key Locations - Supports both file paths and environment variables
smallrye.jwt.verify.key.location=${smallrye.jwt.verify.key.location:publicKey.pem}
smallrye.jwt.sign.key.location=${smallrye.jwt.sign.key.location:privateKey.pem}

# Security Paths
quarkus.http.auth.permission.auth-permit.paths=/auth/*
quarkus.http.auth.permission.auth-permit.policy=permit
quarkus.http.auth.permission.authenticated.paths=/users/*,/movies/*
quarkus.http.auth.permission.authenticated.policy=authenticated
quarkus.http.auth.permission.public.paths=/swagger-ui*,/apis*,/health*
quarkus.http.auth.permission.public.policy=permit

# OpenAPI/Swagger
quarkus.swagger-ui.path=/apis
quarkus.swagger-ui.always-include=true

# Logging Configuration
quarkus.log.level=INFO
quarkus.log.console.level=INFO
quarkus.log.console.format=%d{yyyy-MM-dd HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n
quarkus.log.category."io.smallrye.jwt".level=INFO
quarkus.log.category."io.quarkus".level=INFO
quarkus.log.category."org.hibernate".level=WARN

# External API Configuration
external.api.base-url=${EXTERNAL_API_BASE_URL}

# Quarkus specific
quarkus.package.type=jar
quarkus.container-image.build=true
quarkus.container-image.push=false
quarkus.banner.enabled=true

